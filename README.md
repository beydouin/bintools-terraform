# bintools-terraform

Terraform infrastructure-as-code for the `universe.hm` environment, including VMware vSphere VM and golden-template provisioning.

This repository contains the Terraform layer of the home infrastructure. System configuration and service builds remain managed by Ansible in `bintools-home`.
